% Macros for web version only.
% Corresponding print versions are in macros/print.tex.

% plasTeX does not support \textsc in math mode.
% Override strategy name macros to use \text{} instead.
\renewcommand{\FurnRush}{\text{FurnRush}}
\renewcommand{\WeapRush}{\text{WeapRush}}
\renewcommand{\PeaceFarm}{\text{PeaceFarm}}
\renewcommand{\RubyEcon}{\text{RubyEcon}}
\renewcommand{\MineHeavy}{\text{MineHeavy}}
\renewcommand{\AnimHusb}{\text{AnimHusb}}
\renewcommand{\PeaceCave}{\text{PeaceCave}}
\renewcommand{\Balanced}{\text{Balanced}}
